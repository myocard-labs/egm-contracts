{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/myocard-labs/egm-contracts/schemas/iafdb_bank.schema.json",
  "title": "IafdbBank",
  "description": "Calibrated + band-pass-filtered bipolar EGM segments extracted from PhysioNet IAFDB. Schema versions 1.3-1.4 (new banks are stamped 1.4). Symmetric with SyntheticBank: maps to an HDF5 file with bank-level root attrs plus a `traces/` group containing per-trace columns. No `label` column — labeling is downstream policy applied at ClassifierBank conversion time by a consumer-supplied label_fn. 1.2 (from 1.1) added the optional `bank_id` stable-artifact identifier (egm-contracts v0.5.0, cross-artifact linkage). 1.3 (from 1.2) added the optional `run_record_path` sidecar pointer and the optional per-trace `activation_position` (egm-contracts v0.6.0, Phase 1.5). 1.4 (from 1.3) widened `calibration_method` to admit 'none' so a bank that was not calibrated can say so instead of asserting a step that never ran (egm-contracts v0.6.1). Both versions are accepted on read.",
  "type": "object",
  "properties": {
    "schema_version": {
      "type": "string",
      "enum": ["1.3", "1.4"],
      "description": "Schema version in X.Y form. X (major) bumps at release; Y (minor) bumps on every dev-time structural change. Consumers MUST refuse unknown major versions. 1.1 (from 1.0) added: 'none' to threshold_mode enum + nullable threshold_value, for the unfiltered-export path (every window, no healthy threshold). 1.2 (from 1.1) added the optional `bank_id` stable-artifact identifier (egm-contracts v0.5.0, cross-artifact linkage). 1.3 (from 1.2) added the optional `run_record_path` sidecar pointer + the optional per-trace `activation_position` (egm-contracts v0.6.0). 1.4 (from 1.3) widened `calibration_method` to admit 'none'. ORDER IS LOAD-BEARING: schema_info.current_version() returns the LAST entry, so '1.4' must stay last or new banks would be stamped with the older version. Pre-1.0 of egm-contracts: the enum-of-one pattern per project/schema_evolution.md (no in-the-wild back-compat needed yet); when v1.0 ships, this enum may grow to accept multiple supported versions."
    },
    "created_utc": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 UTC timestamp captured at write time."
    },
    "bank_id": {
      "$ref": "common.schema.json#/$defs/ArtifactId",
      "description": "Stable artifact ID for this bank, e.g. 'upred_iafdb_v1_5_2026-06-25' or 'nbank_iafdb_2026-06-15'. Optional for legacy banks written before egm-contracts v0.5.0; egm-data stamps it on every new bank (enforced at write time, not by this schema). Added 1.2."
    },
    "run_record_path": {
      "type": "string",
      "description": "Relative path to a sibling JSON run record holding this bank's per-record extraction diagnostics, e.g. 'iafdb_healthy_v1_run_record.json'. Relative to the bank file's own directory, mirroring the noise_bank <-> noise_bank_run_record sibling convention. Optional: a bank written without the audit report simply omits it, and readers MUST treat absence as 'no sidecar', not as an error. The record's own schema is deliberately NOT formalized yet (the methods paper may reshape it); until it is, the file is documented-but-unvalidated JSON, which is why this is a path rather than an embedded object. Added 1.3."
    },
    "source": {
      "type": "string",
      "enum": ["iafdb v1.0.0"],
      "description": "Provenance tag identifying the upstream dataset. Bumps when the underlying PhysioNet dataset version changes."
    },
    "fs_hz": {
      "type": "number",
      "enum": [1000.0],
      "exclusiveMinimum": 0,
      "description": "Sampling rate in Hz. IAFDB is fixed at 1 kHz; enum (rather than const) so datamodel-code-generator emits a Literal[1000.0] in Pydantic and a concrete typed enum in C++. The exclusiveMinimum belt-and-suspenders catches malformed values that bypass the enum check."
    },
    "trace_duration_ms": {
      "type": "number",
      "exclusiveMinimum": 0,
      "description": "Per-trace duration in milliseconds (equals window_ms)."
    },
    "calibration_method": {
      "type": "string",
      "enum": ["r_wave_anchoring", "none"],
      "description": "How per-record amplitude calibration was computed, or 'none' when no calibration was applied and the stored signal is in the source dataset's raw units. 'none' was added in 1.4 because a single-member enum left an uncalibrated bank with no legal value to write \u2014 the only value that validated asserted a calibration step had run when it had not, so the file itself would have carried a false claim about its own signal processing. That is the line: a field that would merely be unused can take a sentinel, a field that would be untrue cannot. Which method is appropriate is a scientific question settled per corpus rather than a schema default \u2014 R-wave anchoring scales EGM amplitude against a surface-ECG QRS reference, which is not standard intracardiac practice, so it is a selectable option and not the forced path. Future methods add enum values rather than replacing existing ones."
    },
    "calibration_target_qrs_pp_mv": {
      "type": "number",
      "exclusiveMinimum": 0,
      "description": "Target peak-to-peak QRS amplitude in mV against which each record was calibrated. Stays REQUIRED and strictly positive even when calibration_method is 'none': producers write +inf, the same sentinel convention peak_to_peak_mv and hop_ms already use for not-applicable-in-this-mode. Deliberately not made nullable \u2014 a sentinel in a field that is merely unused costs nothing, while nullable would force every reader to handle a None it can never act on."
    },
    "threshold_mode": {
      "type": "string",
      "enum": ["absolute", "percentile", "none"],
      "description": "Threshold strategy that decided which segments are retained. 'absolute' = fixed mV cap (default 0.5 per Sánchez 2000, or 0.2 per Kosiuk AF-adjusted); 'percentile' = per-record percentile of the calibrated p-p distribution; 'none' (since 1.1) = no filter applied, every windowed segment retained — used for unsupervised pretraining banks where label semantics don't gate selection."
    },
    "threshold_value": {
      "type": ["number", "null"],
      "description": "Value used by the threshold strategy. mV for 'absolute' (e.g., 0.5 per Sánchez); 0..100 percentile for 'percentile'; null when threshold_mode='none' (since 1.1)."
    },
    "band_hz": {
      "type": "array",
      "items": { "type": "number", "exclusiveMinimum": 0 },
      "minItems": 2,
      "maxItems": 2,
      "description": "Band-pass filter edges in Hz, [low, high]. Defaults to the clinical 30-300 Hz band."
    },
    "window_ms": {
      "type": "number",
      "exclusiveMinimum": 0,
      "description": "Sliding-window length in ms. Default 512 ms to match the classifier's T=512 input."
    },
    "window_samples": {
      "type": "integer",
      "exclusiveMinimum": 0,
      "description": "Window length in samples (= window_ms * 1e-3 * fs_hz)."
    },
    "hop_ms": {
      "type": "number",
      "exclusiveMinimum": 0,
      "description": "Stride between adjacent windows in ms. Additive (not in the original format-version-1 spec but recorded for reproducibility; readers MAY ignore it)."
    },
    "source_records": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Record names that contributed at least one segment to this bank (e.g., 'iaf1_afw'). Allows zero entries (empty bank is valid)."
    },
    "traces": {
      "type": "object",
      "description": "Per-trace columns. Maps to the HDF5 `traces/` group. All columns have first dimension N (the segment count) and are aligned.",
      "properties": {
        "signal": {
          "type": "array",
          "items": { "type": "array", "items": { "type": "number" } },
          "description": "(N, window_samples) float32 — calibrated + band-pass-filtered bipolar EGM segments.",
          "x-hdf5-dataset": { "name": "traces/signal", "dtype": "float32", "rank": 2 }
        },
        "patient_id": {
          "type": "array",
          "items": { "type": "string", "pattern": "^iaf[1-8]$" },
          "description": "(N,) UTF-8 — IAFDB patient identifier, one of 'iaf1'..'iaf8'.",
          "x-hdf5-dataset": { "name": "traces/patient_id", "dtype": "utf-8", "rank": 1 }
        },
        "source_record": {
          "type": "array",
          "items": { "type": "string" },
          "description": "(N,) UTF-8 — IAFDB record name (e.g., 'iaf1_afw').",
          "x-hdf5-dataset": { "name": "traces/source_record", "dtype": "utf-8", "rank": 1 }
        },
        "source_channel": {
          "type": "array",
          "items": { "type": "string" },
          "description": "(N,) UTF-8 — Bipolar channel name within the source record (e.g., 'CS12').",
          "x-hdf5-dataset": { "name": "traces/source_channel", "dtype": "utf-8", "rank": 1 }
        },
        "start_sample": {
          "type": "array",
          "items": { "type": "integer", "minimum": 0 },
          "description": "(N,) int64 — Sample offset of the segment into the source record. Combined with patient_id + source_record + window_samples, this uniquely identifies the trace's location in the raw IAFDB data.",
          "x-hdf5-dataset": { "name": "traces/start_sample", "dtype": "int64", "rank": 1 }
        },
        "peak_to_peak_mv": {
          "type": "array",
          "items": { "type": "number", "minimum": 0 },
          "description": "(N,) float32 — Calibrated peak-to-peak amplitude in mV that passed the threshold check. Real-mV interpretation.",
          "x-hdf5-dataset": { "name": "traces/peak_to_peak_mv", "dtype": "float32", "rank": 1 }
        },
        "calibration_scalar": {
          "type": "array",
          "items": { "type": "number" },
          "description": "(N,) float32 — Per-patient scalar applied to this segment during R-wave anchoring. signal_in_mv = signal_raw * calibration_scalar.",
          "x-hdf5-dataset": { "name": "traces/calibration_scalar", "dtype": "float32", "rank": 1 }
        },
        "activation_position": {
          "type": "array",
          "items": { "$ref": "common.schema.json#/$defs/ActivationPosition" },
          "description": "(N,) float32 — Realized activation position within each window, as a [0,1] fraction of the trace (see common.schema.json ActivationPosition for the convention). Written by the activation-aware splitter, which anchors each window on a detected activation; it is what the splitter PRODUCED, not a measurement taken from the waveform afterwards. OPTIONAL, and permanently so — see common.ActivationPosition for the full rule. Absent on sliding-window banks (no anchor exists), on multi-beat traces (no single position describes them), and on activation-mode banks written before the splitter shipped: Phase-1.5 Wave 1 adds this column, Wave 2 (IAF1) populates it. Readers MUST treat absence as 'unknown position', not zero. Its counterpart on synthetic_bank carries the identical definition so the two corpora's position distributions are compared stored-vs-stored. Added 1.3.",
          "x-hdf5-dataset": { "name": "traces/activation_position", "dtype": "float32", "rank": 1 }
        }
      },
      "required": [
        "signal", "patient_id", "source_record", "source_channel",
        "start_sample", "peak_to_peak_mv", "calibration_scalar"
      ],
      "additionalProperties": false
    }
  },
  "required": [
    "schema_version", "created_utc", "source", "fs_hz", "trace_duration_ms",
    "calibration_method", "calibration_target_qrs_pp_mv", "threshold_mode",
    "threshold_value", "band_hz", "window_ms", "window_samples",
    "source_records", "traces"
  ],
  "additionalProperties": false,
  "x-hdf5-mapping": {
    "root_attrs": [
      "fs_hz", "trace_duration_ms", "source", "calibration_method",
      "calibration_target_qrs_pp_mv", "threshold_mode", "threshold_value",
      "band_hz", "window_ms", "window_samples", "hop_ms", "created_utc",
      "schema_version", "bank_id", "run_record_path", "source_records"
    ],
    "traces_group": "traces"
  }
}
